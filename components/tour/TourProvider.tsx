"use client";

// Tour context + 상태 관리
// TODO: 투어 단계(steps) 정의, 현재 단계 상태, 투어 시작/종료 제어
export default function TourProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  // TODO: context 구현
  return <>{children}</>;
}
