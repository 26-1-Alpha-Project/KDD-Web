// Google 로그인 버튼만 있는 웰컴 화면
// 유저플로우: START → (기존 이용자 여부) → 로그인 / 회원가입
// 신규 유저: 서비스 소개(온보딩) → 회원가입 → 프로필 설정 → /chat
// 기존 유저: 로그인 → /chat
export default function LoginPage() {
  return (
    <div>
      <h1>로그인</h1>
      {/* TODO: Google 로그인 버튼 */}
    </div>
  );
}
