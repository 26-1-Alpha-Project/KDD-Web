// 온보딩 페이지 — 웰컴 + 프로필 설정 (첫 로그인 이후)
// 유저플로우: 신규 유저 → 회원가입 → 프로필 설정 → /chat
export default function OnboardingPage() {
  return (
    <div>
      <h1>온보딩</h1>
      {/* TODO: 웰컴 메시지 + 프로필 설정 폼 */}
    </div>
  );
}
